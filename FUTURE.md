今後追加したい機能

- retouch ファイルの同期 (retouch 後でも rating をあわせるため)、なおどの情報を sync するメタデータはconfig ファイルで指定するものとする, コマンドでも指定可にも)
- dng の同期 (基本的には同じディレクトリの raw ファイルの名前を含むものの情報をコピー、サイドカー？)
- 指定したディレクトリをフラットにする方法(jpg/ raw/ ディレクトリの中身を一つのディレクトリに展開する、やり直し用)
- CLI 検索ツール (単一の機能として実装するか分けるかは要検討)
    - 引数 : パス (デフォルトカレントディレクトリ)
        - 機能()    
            - 指定した XMP:Rating の要件を満たすフルパスを返却するコマンド( =, <, 同等、それ以上それ以下などで指定)
            - 指定したラベルがあるファイルのフルパスを返却するコマンド
            - 存在する XMP タグを列挙するコマンド
    